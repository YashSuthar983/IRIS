# IRis Backend API Dependencies

# Web Framework
Flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.0.1

# Machine Learning
torch>=2.2.0
numpy>=1.26.0
scikit-learn>=1.3.2
joblib>=1.3.2

# Utilities
python-dotenv==1.0.0

# Note: LLVM tools (clang, opt, llc) and RISC-V toolchain must be installed separately
# - clang (with LLVM tools)
# - riscv64-linux-gnu-gcc (RISC-V cross-compiler)
# - qemu-riscv64 (QEMU for RISC-V emulation)
